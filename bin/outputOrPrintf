#!/bin/bash

printShortUsage()
{
    # Note: short followed by long option; if the user knows the short one, she can
    # skim the long one.
    printf 'Usage: cat FILE [...] | %q %s\n' "$(basename "$1")" '[-w|--non-whitespace] [-m|--multiline] [-2|--stderr] [--status N] [--] FORMAT [ARGUMENTS ...] [-?|-h|--help]'
}
printUsage()
{
    # This is the short help when launched with no or incorrect arguments.
    # It is printed to stderr to avoid accidental processing.
    printShortUsage "$1" >&2
    printf >&2 '\nTry %q --help for more information.\n' "$(basename "$1")"
}
printLongUsage()
{
    # This is the long "man page" when launched with the help argument.
    # It is printed to stdout to allow paging with 'more'.
    cat <<HELPDESCRIPTION
If output is piped into the command, it is passed through as-is.
If there's no output, printf FORMAT [ARGUMENTS ...] instead and exit with 1.
HELPDESCRIPTION
    echo
    printShortUsage "$1"
    echo
    cat <<HELPTEXT
    --non-whitespace|-w	    Output needs to contain at least one non-whitespace
			    character to be accepted.
    --multiline|-m	    Output needs to consist of more than one line to be
			    accepted.
    --status N		    Fail with N instead of 1.
HELPTEXT
}

redir=
noOutputStatus=1
typeset -a outputOrErrorArgs=()
while [ $# -ne 0 ]
do
    case "$1" in
	--help|-h|-\?)	shift; printLongUsage "$0"; exit 0;;
	--non-whitespace|-w|--multiline|-m)
			outputOrErrorArgs+=("$1"); shift;;
	--stderr|-2)	shift; redir='>&2';;
	--status)	shift; noOutputStatus="${1:?}"; shift;;
	--)		shift; break;;
	-*)		{ echo "ERROR: Unknown option \"$1\"!"; echo; printUsage "$0"; } >&2; exit 2;;
	*)		break;;
    esac
done
if [ $# -eq 0 ]; then
    printUsage "$0" >&2
    exit 2
fi

outputOrError "${outputOrErrorArgs[@]}" && exit 0
eval "printf $redir" '"$@"'
exit $noOutputStatus
